global:
  message: Welcome to Show Service - PROD!
server:
 port: 8087
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/show_prod
    username: root
    password: 
  jpa:
    hibernate:
     ddl-auto: update
     show-sql: true
    properties:
     hibernate:
     dialect: org.hibernate.dialect.MySQL8Dialect
     globally_quoted_identifiers: true
